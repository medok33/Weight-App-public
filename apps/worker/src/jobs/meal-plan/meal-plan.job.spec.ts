import { describe, expect, it } from 'vitest'; import { createMealPlanGenerationJob } from './meal-plan.job'; import { processMealPlanGenerationJob } from './meal-plan.processor'; describe('meal plan job', () => { it('is deterministic and idempotent', () => { const job = createMealPlanGenerationJob('u', [{ id: 1 }], 'k'); expect(processMealPlanGenerationJob(job)).toMatchObject({ idempotencyKey: 'k', days: 1, status: 'completed' }); }); });
