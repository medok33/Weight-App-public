import { describe, expect, it } from 'vitest'; import { adherenceScore } from '../domain/progress.policy'; describe('progress', () => { it('calculates adherence', () => expect(adherenceScore(3, 4).score).toBe(75)); });
