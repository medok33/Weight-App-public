import { describe, expect, it } from 'vitest'; import { aggregateIngredients, roundPackages } from '../domain/shopping-list.policy'; describe('shopping list policy', () => { it('aggregates and rounds packages', () => { expect(roundPackages(550, 500)).toBe(1000); expect(aggregateIngredients([{ productId: 'p', quantity: 550, unit: 'g', packageSize: 500 }, { productId: 'p', quantity: 200, unit: 'g' }])).toEqual([{ productId: 'p', quantity: 1200, unit: 'g' }]); }); });
