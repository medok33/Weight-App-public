import { describe, expect, it } from 'vitest'; import { validateOffer } from '../domain/payments.policy'; import { validateCreatePaymentRequest, validateProviderName } from '../domain/payment-provider.policy'; import type { PaymentProvider } from '../domain/payment-provider.types'; describe('product offers and provider contract',()=>{it('validates pricing configuration',()=>{expect(validateOffer({key:'pro-monthly',name:'Pro',amountMinor:9900,currency:'RUB',interval:'month',active:true,metadata:{}}).amountMinor).toBe(9900);expect(()=>validateOffer({key:'BAD',name:'',amountMinor:-1,currency:'GBP',interval:'day' as 'month',active:true,metadata:{}})).toThrow('PRODUCT_OFFER_INVALID');});it('accepts provider keys and secure requests',()=>{expect(validateProviderName('mock')).toBe('mock');expect(()=>validateProviderName('')).toThrow('PAYMENT_PROVIDER_UNSUPPORTED');expect(validateCreatePaymentRequest({userId:'u',amountMinor:9900,currency:'RUB',returnUrl:'https://example.test/return',idempotencyKey:'idem-1'}).idempotencyKey).toBe('idem-1');expect(()=>validateCreatePaymentRequest({userId:'u',amountMinor:1,currency:'RUB',returnUrl:'http://insecure',idempotencyKey:'x'})).toThrow('PAYMENT_PROVIDER_REQUEST_INVALID');});it('defines a provider-compatible structural contract',()=>{const provider: PaymentProvider={name:'telegram',createPayment:async()=>({providerPaymentId:'p',status:'pending'}),refund:async()=>({providerRefundId:'r',status:'pending'}),verifyWebhook:()=>({providerEventId:'e',type:'payment.pending',payload:{}})};expect(provider.name).toBe('telegram');});});
