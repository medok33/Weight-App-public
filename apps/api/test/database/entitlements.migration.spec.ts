import { describe, expect, it } from 'vitest'; import fs from 'node:fs'; import path from 'node:path'; describe('entitlements migration',()=>{it('creates durable access table and indexes',()=>{const sql=fs.readFileSync(path.join(process.cwd(),'prisma/migrations/135_entitlements/migration.sql'),'utf8');expect(sql).toContain('CREATE TABLE "Entitlement"');expect(sql).toContain('UNIQUE ("userId", "key")');expect(sql).toContain('Entitlement_userId_status_endsAt_idx');});});
