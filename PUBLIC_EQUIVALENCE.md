# Public equivalence evidence

Source commit: $sourceSha

The machine-readable manifest is PUBLIC_EQUIVALENCE_MANIFEST.json. It records SHA-256 for every exported file that maps to the exact source snapshot, all excluded tracked source paths, and all public-only additions/deltas. Product-code mappings must have match: true; public-only files are explicitly listed.

Confirmed secrets: 0
Likely real secrets: 0
