# Responsive breakpoints

Use semantic layout tokens rather than device-specific assumptions:

| Range | Layout |
|---|---|
| 0–639px | Single column, bottom navigation |
| 640–1023px | Two-column content where safe |
| 1024px+ | Persistent navigation and max-width content |

The same route and state model applies to web, iOS, and Android clients.
